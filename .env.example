# Base URL of the Django API.
# Local dev:
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api
# Production (Vercel) — point at the deployed API behind Cloudflare:
# NEXT_PUBLIC_API_URL=https://api.survivalexplores.com/api
